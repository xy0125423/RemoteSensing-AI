# RemoteSensing-AI

AI 接管说明（AI Context）

«如果你是新加入本项目的 AI，请先阅读本节，再阅读本文档剩余内容。»

项目定位

这是一个本科阶段的长期工程项目，而不是一次性的课程作业。

目标是在 2026 年 12 月 前，完成一个可运行、可演示、可持续扩展的遥感自动化分析系统，用于：

- 考研复试（西安电子科技大学遥感方向）
- 求职作品集
- 工程能力培养

项目强调工程化，而不是追求论文创新。

---

我的背景

- 本科专业：遥感科学与技术
- 数学基础较好
- 正在准备考研（专业课：信号与系统）
- 没有 NVIDIA GPU
- 使用 Google Earth Engine 获取遥感数据
- 当前以 CPU 开发为主

---

项目目标

最终实现：

输入研究区域（地名、坐标或矢量边界）

↓

自动获取遥感数据

↓

完成指数计算（NDVI、NDWI、EVI 等）

↓

完成时间序列分析

↓

完成傅里叶频谱分析

↓

自动生成图表

↓

自动生成 PDF 报告

整个流程尽量做到一键运行。

---

当前开发原则（最高优先级）

当以下原则发生冲突时，按顺序执行：

1. 能跑 > 完美
2. 工程化 > 炫技
3. 简单 > 复杂
4. 文档 > 聊天记录
5. 可维护 > 一次性完成

任何建议都应尽量遵循以上原则。

---

当前明确不做

在考研结束之前，不建议把精力投入到：

- 深度学习
- 大模型训练
- Web 前端
- 数据库
- 云服务器部署
- 复杂 GUI
- 与当前路线无关的新框架

如果未来确实需要，请明确说明原因，而不是默认引入。

---

AI 的协作方式

请把自己视为项目技术负责人，而不是代码生成器。

回答问题时，请优先：

1. 理解当前项目结构。
2. 保持与已有架构一致。
3. 不轻易推翻已有设计。
4. 尽量修改已有代码，而不是重写整个项目。
5. 每一步都保证项目保持可运行状态。

如果需要增加新的依赖、目录或模块，请说明：

- 为什么需要；
- 会带来什么收益；
- 是否值得当前阶段引入。

---

输出要求

默认情况下，请：

- 优先给出工程实践建议；
- 解释设计原因；
- 保持代码规范；
- 保持文档同步更新；
- 不为了追求"高级"而增加复杂度。

如果存在多个方案，请比较优缺点，并说明推荐理由。

---

当前阶段

请阅读 TODAY.md。

所有建议都应围绕 TODAY.md 当前任务展开，不要跳跃开发，也不要提前引入未来阶段的内容。

如果 TODAY.md 与 PROJECT.md 冲突，以 TODAY.md 的当前任务为准。

---

接管成功标准

如果你已经理解以上内容，请继续阅读 PROJECT.md，并以该项目已有架构继续协助开发，而不是重新设计整个项目。

> 输入任意研究区域（地名、坐标或矢量边界）→ 自动获取遥感数据 → 完成分析 → 自动生成专业 PDF 报告。

面向考研复试和求职作品集的遥感自动化工程体系，不依赖GPU。

## 快速启动

```bash
git clone https://github.com/yourname/RemoteSensing-AI
cd RemoteSensing-AI
pip install -r requirements.txt
python main.py --region 广西





## 第一战：GeoTIFF 驯服战（2026-07-02 ~ 2026-07-03）

### 已完成

✅ 工程环境配置

✅ Google Earth Engine 基础

✅ Geometry / ImageCollection / filterBounds() / filterDate() / Cloud Filter

✅ ImageCollection → Image（first()）

✅ Map.addLayer() 真彩色显示

✅ Export.image.toDrive() 导出

✅ GeoTIFF 已放入 data/raw/sentinel2_sample.tif

当前成果：

从全球 Sentinel-2 数据集中，筛选出郓城县 2025 年云量 <10% 的 28 张影像，成功导出第一张 GeoTIFF 到本地。

### 下一阶段

- rasterio 读取 GeoTIFF 元数据
- 提取 B4/B8 为 numpy 数组
- matplotlib 显示真彩色影像（本地）